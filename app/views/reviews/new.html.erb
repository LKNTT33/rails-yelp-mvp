<h1>New review</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.label :rating %>
  <%= f.select(:rating, %W[0 1 2 3 4 5]) %>
  <%= f.label :content %>
  <%= f.input :content %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
